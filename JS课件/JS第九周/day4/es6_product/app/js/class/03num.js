{//num
    //01:类上提供的很多方法：MAX_SAFE_INTEGER,MIN_SAFE_INTEGER,isSateInteger,isInteger,toFixed()
    let num=234;
    //console.log(Number.MAX_SAFE_INTEGER)
    //console.log(Number.MIN_SAFE_INTEGER);
    //console.log(Number.isSafeInteger(num/0));
    //console.log(Number.isInteger(num));
    //console.log(Number.isFinite(num/0))
}
{//Math:round(); sqrt(),pow(),abs(),random(),floor,ceil,max(),min();
    //Math.cbrt()开立方
    console.log(Math.cbrt(8));
    //Math.sign()
    console.log(Math.sign(0));
    //Math.trunc()
    console.log(Math.trunc(4.99))
}
